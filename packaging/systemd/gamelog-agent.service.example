# Example only: validate service behavior for user sessions (especially Wayland) before use.
[Unit]
Description=GameLog game detection agent

[Service]
Type=simple
ExecStart=/usr/bin/gamelog-agent
Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
